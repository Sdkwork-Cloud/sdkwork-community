version: 1
defaultProfile: standalone.unified-process.development

profiles:
  standalone.unified-process.development:
    expose:
      - domain: localhost
        tls: off
        mode: api
    packages: []

  standalone.unified-process.production:
    install:
      layout: binary-package
    expose:
      - domain: localhost
        tls: off
        mode: api
    packages: []
